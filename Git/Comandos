git config --global (cambios globales y no por proyecto) 
git config --global core.editor "code --wait" (declarar vs code como editor de texto predeterminado) (--wait para esperar a que se cierre el editor de texto)
pwd (muestra donde esta ubicado)
cd (entra en una ubicacion)
cd .. (sale de la ubicacion hacia atras en un nivel)
git add . (agrega todos los archivos) git add "..." (agrega el archivo especificado) git add *.txt (agrega todos los archivos con el mismo formato) git add . (agrega todo)
git add archivo1 archivo2 (agrega todos los archivos seleccionados) 
rm "..." (elimina el archivo seleccionado) git rm "..." (elimina el archivo sin add)
git restore ... (recupera archivo seleccionado eliminado)
git mv ... ... (cambia el nombre del primer archivo por el segundo)
git branch (muestra las ramas que hay)
git branch ... (crea una rama con el nombre especificado)
git branch -m ... ... (cambia el primer nombre de la rama por el segundo)
$ git branch -d ... (elimina la rama seleccionada)

